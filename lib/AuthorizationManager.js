


	var Class = require( "ee-class" )
		, log = require( "ee-log" );



	module.exports = new Class( {


		init: function(){
			
		}


		// add yuthorization middleware
		, use: function( options, middleware ){
			
		}

		
		// accept only authenticated requests, you define exceptions
		, request: function( request, response, next ){
			
		}
	} );